# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Notebook execution
nbformat>=5.10.0
nbclient>=0.10.0
jupyter-client>=8.6.0
ipykernel>=6.29.0

# Scientific Python (course curriculum — numpy, pandas, scipy, etc.)
numpy>=2.1.0
pandas>=2.2.0
scipy>=1.14.0
scikit-learn>=1.5.0
matplotlib>=3.9.0
seaborn>=0.13.0
sympy>=1.13.0

# Machine learning (PyTorch — used in curriculum assignments)
torch>=2.4.0

# Utilities
sigfig>=1.3.0
pytest>=8.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# HTTP client (for potential callback patterns)
httpx>=0.28.0
